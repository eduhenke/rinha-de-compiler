//let fib = fn (n) => {
//  if (n < 2) {
//    n
//  } else {
//    fib(n - 1) + fib(n - 2)
//  }
//};
//print ("fib " + fib(250000))
let fact = fn (n) => {
  if (n < 2) {
    1
  } else {
    n * fact(n - 1)
  }
};
print(fact(12))